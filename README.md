# SPEECH-RECOGNITION-SYSTEM
 This project builds a basic speech-to-text system using Python and the SpeechRecognition library. It converts short audio clips into text using a pre-trained Google model and displays the transcribed output.
